# Tea-Plantation-POS
This is a point of sales application developed using C# with Microsoft SQL Server.
Database has a list of employees that works in the tea plantation industry and the
application calculates the amount they should be paid according to the weight of the
tea leaves that they have collected (in kilos).
All the payments are then recorded in another table for record keeping with date and time.
